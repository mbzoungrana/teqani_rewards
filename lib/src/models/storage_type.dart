/// Enum representing different storage types supported by Teqani Rewards
enum StorageType {
  /// Use SharedPreferences for storage
  sharedPreferences,
  
  /// Use SQLite for storage
  sqlite,
  
  /// Use Hive for storage
  hive,
  
  /// Use Firebase for storage
  firebase
} 